const Partner = (props) => (
    <section>
        <div>
            { props.partner.name }
        </div>
        <style jsx>
            {`

            `}
        </style>
    </section>
)

export default Partner