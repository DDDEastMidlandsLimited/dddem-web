const Sponsor = (props) => (
    <section>
        <div>
            { props.sponsor.name }
        </div>
        <style jsx>
            {`

            `}
        </style>
    </section>
)

export default Sponsor