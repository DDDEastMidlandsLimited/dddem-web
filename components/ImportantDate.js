const ImportantDate = (props) => (
    <section>
        <div>
            { props.dates.name } - { props.dates.date }
        </div>
        <style jsx>
            {`

            `}
        </style>
    </section>
)

export default ImportantDate