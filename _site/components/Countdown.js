const Countdown = (props) => (
    <section>
        <div>
            { props.countdown }
        </div>
        <style jsx>
            {`

            `}
        </style>
    </section>
)

export default Countdown